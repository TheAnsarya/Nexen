name: 'Write commit SHA1 to file'
description: 'Write the current commit SHA1 to a build file for version identification'
runs:
  using: "composite"
  steps:
      - name: Write commit SHA1 to file
        shell: bash
        run: printf '%s' "${{ github.sha }}" > UI/Dependencies/Internal/BuildSha.txt
