# Issue #1296 v1.4.32 Stabilization And Release Plan

## Context

Issue: [#1296](https://github.com/TheAnsarya/Nexen/issues/1296)

Requested release workflow:

- Commit all current pending changes.
- Stabilize build and fix as many warnings as possible.
- Create and publish release v1.4.32.
- Trigger GitHub Actions artifact build/release workflow.
- Provide comprehensive release title and description.

## Goals

- Capture warning baseline and reduce warnings where feasible.
- Ensure working Release x64 build and full test pass.
- Update release metadata to v1.4.32 (version fields, changelog, README download links, release notes).
- Commit and push all pending local changes as requested.
- Publish v1.4.32 and dispatch release build workflow.

## Implemented Actions

1. Created issue #1296 and prompt-tracking comment.
2. Performed clean Release x64 build and warning scan.
3. Updated release metadata for v1.4.32:
	- `UI/UI.csproj` version fields.
	- `CHANGELOG.md` new `1.4.32` entry.
	- `README.md` download links and quick-start assets.
	- `.github/release-notes/latest.md` comprehensive release body.
4. Created this plan and a session log entry.
5. Validated with full C++ + managed test runs.
6. Commit all pending local changes.
7. Tag `v1.4.32`, push, and dispatch build workflow with `run_release=true` and `release_tag=v1.4.32`.

## Warning/Stability Notes

- Local clean Release build warning scan returned no warning lines in `build_output_v1432.txt`.
- Stabilization focus remained on preserving current zero-warning local baseline while shipping queued reliability fixes from #1289/#1290/#1291/#1295.

## Validation Targets

- `MSBuild.exe Nexen.sln /p:Configuration=Release /p:Platform=x64 /t:Build`
- `Core.Tests.exe --gtest_brief=1`
- `dotnet test Tests/Nexen.Tests.csproj -c Release --nologo -v m`

## Release Execution

- Tag: `v1.4.32`
- Release name/title: `Nexen v1.4.32 - Stability, Settings Reliability, and Pause/OSD UX Refinement`
- Workflow dispatch: `build.yml` with release inputs enabled.
